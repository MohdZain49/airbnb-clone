<form action="/favourites" method="POST">
  <input type="hidden" value="<%= home._id%>" name="id" />
  <button
    class="bg-red-500 text-white  py-2 px-4 rounded-lg hover:bg-red-600 transition duration-200 font-semibold"
  >
    Add to favourite
  </button>
</form>
